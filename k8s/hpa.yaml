apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: AI-Ops-Remediator-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: AI-Ops-Remediator
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 60
